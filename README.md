# Encrypted family trip site

This public repository contains only an AES-encrypted HTML deployment. The
plaintext source, trip details, names, and password are not stored here.

The site is intentionally excluded from search indexing. Updates are built
from a separate private source repository owned by `NevoLevi`.
